"""Catalog Image Builder — POS fork — see README.md, HARVESTING.md, PLAN.md.

Stages: new-customer, harvest (curated input -> downloads + manifest), names,
process, csv, review, upload, run. No wm stage - forked from
catalog_image_builder for POS retailers (Reeds, DSW, ...) instead of
Manhattan WM.
"""

__version__ = "0.1.0"
